#! /usr/local/bin/node

"use_strict";

var app = require('../app.js');
var debug = require('debug');
app.listen(app.get('port'), function() {
  debug('boot:express')('app started on port', app.get('port'));
});
