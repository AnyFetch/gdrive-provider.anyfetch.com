#! /usr/local/bin/node

"use_strict";

var app = require('../app.js');
var debug = require('debug');
app.listen(app.config.port, function() {
  debug('boot:restify')('app started on port', app.config.port);
});
